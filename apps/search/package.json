{
  "name": "@apps/search",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev",
    "build": "pnpm sync && wrangler deploy --dry-run --outdir=dist",
    "deploy": "pnpm sync && wrangler deploy",
    "sync": "tsx scripts/sync-products.ts"
  },
  "dependencies": {
    "@orama/orama": "^3.1.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "dotenv": "^16.5.0",
    "tsx": "^4.19.2",
    "wrangler": "^4.104.0"
  }
}
